var options = {"fiat":"USD", "fiatSymbol": "$"}
var $ = require('jquery');
exports.init = function(container) {
  if (typeof something === "undefined") {
    console.log('container not defined, initializing to body')
    var container = window.$(body);
    container.text('asd');
  }
}


